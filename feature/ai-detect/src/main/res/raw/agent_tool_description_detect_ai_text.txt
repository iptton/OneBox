检测一段文本是否疑似由 AI 生成（朱雀 AIGC 检测），返回疑似 AI 占比、判定结论与分段标签。

使用场景：
- 用户想判断一段文章、文案、作业、论文是否由 AI 写作
- 用户粘贴一段文字并询问"这是不是 AI 写的"、"AI 味重不重"
- 改写/降 AI 味前后对比检测

注意：
- 只接受纯文本内容；如需检测图片请使用 detect_ai_image
- 检测结果为概率性参考，不能作为绝对结论，回复用户时应保留这一限定
