检测一张图片是否疑似由 AI 生成（朱雀 AIGC 检测），返回 AI 生成置信度与判定结论。

使用场景：
- 用户想判断一张图片是否为 AI 绘画 / AI 生成图
- 用户提供图片并询问"这张图是不是 AI 生成的"、"鉴别图片真伪"

注意：
- image_uri 支持 content:// 与 file://，可从对话附件或 browse_files 工具的结果中获取
- 图片需大于 300×300 且不超过 10MB，应用必须能读取该 URI
- 检测结果为概率性参考，不能作为绝对结论，回复用户时应保留这一限定
