读取当前浏览器页面的内容。支持四种模式：full_text（提取页面纯文本，适合内容分析）、dom（提取指定 CSS 选择器元素的 HTML，默认 body）、info（返回 URL、标题、导航状态等元信息）、snapshot（返回包含文本预览的完整页面快照）。maxLength 控制文本截断长度，默认 8192。适合在浏览器导航后获取页面内容进行分析。

工具边界：只读"当前已打开的浏览器页面"，通常先用 browser_navigate 打开目标页；如果只是抓取一个静态 URL 的内容且无需 JS 渲染，优先用 fetch_webpage（更轻量）。